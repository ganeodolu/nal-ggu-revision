/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  styledComponents: true,
  i18n: {
    locales: ["ko"],
    defaultLocale: "ko"
  }
};

module.exports = nextConfig;
